{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/theopenlane/courier/pkg/engine/settings",
  "properties": {
    "host": {
      "type": "string",
      "description": "Host is the base URL of the Openlane API"
    },
    "token": {
      "type": "string",
      "description": "Token is the API token used for authentication"
    },
    "organization-id": {
      "type": "string",
      "description": "OrganizationID optionally scopes requests to a specific organization, only needed for multi-organization tokens"
    },
    "dir": {
      "type": "string",
      "description": "Dir is the directory holding the exported files"
    }
  },
  "additionalProperties": false,
  "type": "object",
  "description": "Settings are the connection and store settings for the binary"
}
